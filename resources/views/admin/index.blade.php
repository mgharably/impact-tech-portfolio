@extends('admin.layout')
@section('content')<div class="section-head"><h1>{{ $type==='projects'?'المشاريع':'الخدمات' }}</h1><a class="button" href="/admin/{{ $type }}/create">+ إضافة {{ $type==='projects'?'مشروع':'خدمة' }}</a></div>@forelse($items as $item)<article class="admin-row"><div><h3>{{ $item->title }}</h3><span class="chip">{{ $item->published?'منشور':'مسودة' }}</span><span class="fine">الترتيب: {{ $item->sort_order }}</span></div><div class="actions"><a href="/admin/{{ $type }}/{{ $item->id }}/edit" class="button secondary small">تعديل</a><form method="post" action="/admin/{{ $type }}/{{ $item->id }}" data-confirm="حذف هذا العنصر نهائيًا؟">@csrf @method('DELETE')<button class="danger">حذف</button></form></div></article>@empty<div class="form-panel"><h2>أضف أول {{ $type==='projects'?'مشروع':'خدمة' }}</h2><p>المشاريع المنشورة فقط ستظهر في الموقع. أضف معلومات موثّقة عن أعمال الشركة.</p></div>@endforelse @endsection
