@extends('admin.layout')
@php($labels=['projects'=>['المشاريع','مشروع'],'team'=>['الفريق','عضو فريق'],'services'=>['الخدمات','خدمة']])
@section('content')<div class="section-head"><h1>{{ $labels[$type][0] }}</h1><a class="button" href="/admin/{{ $type }}/create">+ إضافة {{ $labels[$type][1] }}</a></div>@forelse($items as $item)<article class="admin-row"><div><h3>{{ $item->title ?? $item->name }}</h3><span class="chip">{{ $item->published?'منشور':'مسودة' }}</span><span class="fine">الترتيب: {{ $item->sort_order }}</span></div><div class="actions"><a href="/admin/{{ $type }}/{{ $item->id }}/edit" class="button secondary small">تعديل</a><form method="post" action="/admin/{{ $type }}/{{ $item->id }}" data-confirm="حذف هذا العنصر نهائيًا؟">@csrf @method('DELETE')<button class="danger">حذف</button></form></div></article>@empty<div class="form-panel"><h2>أضف أول {{ $labels[$type][1] }}</h2><p>العناصر المنشورة فقط تظهر في الموقع وملف PDF.</p></div>@endforelse @endsection
