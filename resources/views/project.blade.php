@extends('layout')
@section('title',$project->title.' | Impact Tech')
@section('body')<main class="wrap section case"><a href="/#work" class="text-link">→ جميع الأعمال</a><p class="eyebrow">{{ $project->category }}</p><h1>{{ $project->title }}</h1><p class="large-copy">{{ $project->summary }}</p>@if($project->image)<img class="case-image" src="{{ asset('storage/'.$project->image) }}" alt="{{ $project->title }}">@endif<div class="case-grid">@foreach(['problem'=>'المشكلة','solution'=>'الحل','services'=>'الخدمات المقدمة','technologies'=>'التقنيات','result'=>'النتيجة'] as $key=>$label)@if($project->$key)<article class="service-card"><h2>{{ $label }}</h2><p class="preline">{{ $project->$key }}</p></article>@endif @endforeach</div>@if($project->url)<a class="button" href="{{ $project->url }}" target="_blank" rel="noopener noreferrer">زيارة المشروع ↗</a>@endif</main>@endsection
